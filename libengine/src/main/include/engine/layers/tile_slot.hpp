#pragma once

#include "engine/layers/image_view.hpp"

namespace engine {
    namespace layers {
        struct tile_slot {            
            engine::layers::image_view view;            
        };
    }
}