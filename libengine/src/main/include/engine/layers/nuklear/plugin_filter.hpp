#pragma once

namespace engine {
    namespace layers {
        namespace nuklear {
            enum class plugin_filter {
                DEFAULT,
                ASCII,
                FLOAT,
                DECIMAL,
                HEX,
                OCT,
                BINARY
            };
        }
    }
}