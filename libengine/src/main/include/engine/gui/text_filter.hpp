#pragma once

namespace engine {
    namespace gui {
        enum class text_filter {
            DEFAULT,
            ASCII,
            FLOAT,
            DECIMAL,
            HEX,
            OCT,
            BINARY
        };
    }
}